import { BrowserRouter, Routes, Route } from "react-router-dom"

const RouteSwitch = () => {
    return (
        <BrowserRouter>
            <Routes>

            </Routes>
        </BrowserRouter>
    )
}

export default RouteSwitch